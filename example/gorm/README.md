# Example for GORM + SQLite OpenTelemetry instrumentation

To run this example:

```go
UPTRACE_DSN="https://<token>@api.uptrace.dev/<project_id>" go run .
```

See [documentation](https://docs.uptrace.dev/opentelemetry/go-gorm.html) for details.
