# Example for otelsql database/sql instrumentation

To run this example:

```bash
UPTRACE_DSN="https://<token>@api.uptrace.dev/<project_id>" go run .
```

See [otelsql](https://github.com/uptrace/uptrace-go/tree/master/extra/otelsql) for documentation.
